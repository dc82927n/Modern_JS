const notes = ['Note 1', 'Note 2', 'Note 3']
console.log(notes.length); 
// create an array with five todos
// you have x todos
// print the first and second to last 
let todos = ["walk", "the", "house", "to bed", "fun"]; 
console.log(todos[0] + todos[todos.length-1])
console.log(`${todos[0]}  ${todos[todos.length-1]}`)


